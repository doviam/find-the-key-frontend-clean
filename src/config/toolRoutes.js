/**
 * Rutas públicas de herramientas (React Router).
 * Una sola fuente de verdad: mismos paths en App.jsx y en enlaces (LabToolsSection).
 */
export const TOOL_PATHS = {
  synth: "/synth",
  detector: "/detector",
  findthefrequency: "/findthefrequency",
};
